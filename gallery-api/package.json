{
  "name": "gallery-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/local.js",
    "start": "node --env-file=.env src/local.js",
    "build": "node build.mjs",
    "deploy": "npm run build && bunny scripts deploy dist/bundle.js"
  },
  "dependencies": {
    "@hono/node-server": "2.1.0",
    "@libsql/client": "0.17.4",
    "hono": "4.13.0"
  },
  "devDependencies": {
    "@bunny.net/edgescript-sdk": "^0.12.1",
    "esbuild": "^0.28.1"
  }
}
